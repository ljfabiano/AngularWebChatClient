package com.tiy;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class AngularWebChatClientApplication {

	public static void main(String[] args) {
		SpringApplication.run(AngularWebChatClientApplication.class, args);
//		Build a chat server (hint: use one of your existing baselines for the chat server) Done!
//		Build a simple chat client that can send a single message to your chat server Done!
//		Have a unit test that validates that your chat client works properly DONE!
//		Build a simple controller and HTML page that will be your "home" page DONE!
//		Create a simple JSON endpoint that takes in a message as a query string parameter and returns the history of
// 			all messages that have been sent to the server thus far
//		Make sure your JSON endpoint works by testing it using requests directly from the browser
//		Create an AngularJS controller and use it to add elements to your "home" page to:
//		Let users send a new message
//		Let users see a history of all previous messages that were sent
	}
}
